# Findings this repository has deliberately accepted, by fingerprint.
#
# Fingerprints, not paths or rules: allowlisting a whole file would suppress the
# *next* secret committed to it too, which is the failure mode that makes a
# secret scanner decorative. Each line below pins one occurrence in one commit.
#
# Get a fingerprint from the scanner itself:
#   gitleaks detect --source . --redact --verbose
#
# Format:  <commit-sha>:<path>:<rule-id>:<line>

# ── Accepted: public by design ───────────────────────────────────────────
#
# `NEXT_PUBLIC_POSTHOG_KEY` is a PostHog *project* key. The `NEXT_PUBLIC_`
# prefix means it is inlined into the browser bundle at build time, so it is
# published to every visitor by design and grants nothing. It identifies the
# project; it does not authorise anything against it.
ecf5cfe209782ec619052d1e36d9b398d16e428b:apps/web/.env.example:generic-api-key:31

# ── Accepted: a test fixture, and a pleasing one ─────────────────────────
#
# `'sk-live-abc123'` in a test asserting that the Langfuse masker redacts an
# unknown field. A secret-detection test tripping a secret detector is the
# scanner working correctly on a string that was never a secret.
6ac2f7bf573d9ddbf1c9cf01ea1d0d1b4fd2397f:apps/api/test/langfuse-mask.test.ts:generic-api-key:54

# ── NOT accepted yet: a real credential, awaiting rotation ───────────────
#
# `TIKTOK_CLIENT_KEY` and `TIKTOK_CLIENT_SECRET` were committed with real
# values in `ecf5cfe`. They have since been emptied from the working tree,
# which does **not** undo the exposure — the values are still readable in that
# commit by anyone who can read the repository.
#
# These two lines are left commented ON PURPOSE. Uncommenting them would turn
# the secret-scan job green while a live credential sits in history, which is
# precisely the state a scanner exists to prevent. CI stays red until someone
# does the real work.
#
# To close it:
#   1. Rotate the client secret in the TikTok developer console. Until this is
#      done nothing below matters — the value in history stays usable.
#   2. Put the new secret in Key Vault as `tiktok-client-secret` (never here,
#      never in `.env.example`).
#   3. Then uncomment the two lines, with the rotation date, so the record says
#      the historical value is inert rather than unnoticed.
#
# Rewriting history to excise them is an option and mostly theatre: any clone
# or fork taken before the rewrite keeps the old objects, so rotation is the
# fix either way.
#
# Rotated on: ____________  (fill in, then uncomment)
# ecf5cfe209782ec619052d1e36d9b398d16e428b:apps/api/.env.example:generic-api-key:114
# ecf5cfe209782ec619052d1e36d9b398d16e428b:apps/api/.env.example:generic-api-key:115
