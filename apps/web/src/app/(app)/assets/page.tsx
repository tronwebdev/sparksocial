import { PlaceholderCanvas } from '@/components/shell/PlaceholderCanvas';

export default function AssetsPage() {
  return <PlaceholderCanvas title="Assets Library" subtitle="Everything this brand can make content from" phase="P2" />;
}
