import { PlaceholderCanvas } from '@/components/shell/PlaceholderCanvas';

export default function AutomationPage() {
  return <PlaceholderCanvas title="Automation Recipes" subtitle="Work that runs unattended" phase="P5" />;
}
