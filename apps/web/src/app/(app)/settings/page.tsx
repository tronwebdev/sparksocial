import { PlaceholderCanvas } from '@/components/shell/PlaceholderCanvas';

export default function SettingsPage() {
  return <PlaceholderCanvas title="Settings" subtitle="Workspace and personal" phase="P3" />;
}
